version: "3.9"

services:
  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: pgvault-backend
    restart: unless-stopped
    environment:
      PG_HOST: host.docker.internal
      PG_PORT: 5442
      PG_DATABASE: tiendadb
      PG_USER: tienda
      PG_PASSWORD: tienda_demo
      LOG_LEVEL: info
    ports:
      - "8000:8000"
    command: >
      sh -c "uvicorn app.main:app --host 0.0.0.0 --port 8000"

networks:
  default:
    name: pgvault_net